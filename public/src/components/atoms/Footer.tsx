import styles from './Footer.module.scss';
import { VFC } from 'react';
import { Box } from '@chakra-ui/react';

const Footer: VFC = () => {
  return <Box bg="LimeGreen.100" h="5em"></Box>;
};

export default Footer;
